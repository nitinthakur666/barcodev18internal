<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_barcode_india_lead_type" model="ir.actions.act_window">
        <field name="name">Lead Type</field>
        <field name="res_model">lead.type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">list</field>
        <field name="domain"></field>
    </record>
    <record model="ir.ui.view" id="barcode_india_lead_type">
        <field name="name">Lead Type</field>
        <field name="model">lead.type</field>
        <field name="arch" type="xml">
            <list string="Lead Type" editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

</odoo>